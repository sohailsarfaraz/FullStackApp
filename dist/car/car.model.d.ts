export declare class Car {
    id: number;
    brand: string;
    color: string;
    model: string;
    vin: string;
    constructor(id: number, brand: string, color: string, model: string, vin: string);
}
